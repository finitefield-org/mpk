interface:
  display_name: "Task Work Loop"
  short_description: "Implement, review, fix, commit, and push one task"
  default_prompt: "Use $task-work-loop to implement one project task, review and fix it until clean, then commit and push."
