# Repository instructions

- Git コマンドは常に `/usr/bin/git` を使用してください。
- Git の認証情報は macOS キーチェーンに保存されているため、状態確認を含む
  Git 操作はサンドボックス外で実行してください。
- このリポジトリに必要な Git 操作はすべて許可されています。
- GitHub Actions や workflow ファイルを作成、更新、実行、監視、依存しないで
  ください。`.github/workflows/` は置かないでください。
- `gh run`、`gh workflow`、`workflow_dispatch` は使用しないでください。
- 編集後のテストは、可能な限り、変更によって影響を受けた可能性のある
  テスト項目に絞ってください。影響が疑わしい項目は対象に含め、影響がないと
  判断できる項目の不要な再実行は避けてください。検証記録には対象を選んだ
  理由を残してください。T 完了時の全体ゲートは以下の規則に従ってください。
- 検証はローカルで実行してください。T の途中の W では、変更箇所と
  影響する利用側の対象テスト・必要な lint/format 確認・レビューを実行し、
  `./scripts/check-fast.sh` は実行しないでください。途中の W で同等の全体検証を
  個別コマンドに分けて実行することも避けてください。
- `./scripts/check-fast.sh` は T の最後の W で、その T 全体の変更に対して
  実行してください。失敗時は修正と必要な再検証を行い、通過するまで最後の W
  と T を完了扱いにせず、次の T に進まないでください。
- 各 W の記録では、対象検証の結果と全体ゲートを T 完了時に繰り越したことを
  区別してください。ユーザーが明示的に全体ゲートの実行を依頼した場合は
  その指示を優先します。リリース確認には README に記載されたローカル
  Linux ゲートを使用してください。
