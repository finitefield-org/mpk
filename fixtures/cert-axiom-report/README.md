# Certificate axiom report fixtures

These fixtures pin recomputed axiom reports from `mpk-cert`. They are compact
text snapshots of the report fields that must remain deterministic across
clean runs.
