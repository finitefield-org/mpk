# Policy profile fixtures

`rust-release-policy.json` is the active Rust release selection paired with
`fixtures/package-manifest/valid/rust-policy-package.json`. The package permits
exactly the evidence checker and axiom profiles selected by this release row.
