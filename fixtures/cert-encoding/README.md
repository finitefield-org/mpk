# Certificate encoding fixtures

These fixtures pin canonical `.mpcert` byte encodings. Hex fixtures are decoded
by `mpk-cert` unit tests before comparison so the checked artifact is the byte
sequence, not pretty-printed text.
