# CSHARP-03-T05-W06 review

Scope: cumulative boundary/transition emission and independent import, total
root/callee/loop attachment, existing specialization reuse, source barriers,
byte evidence, retained fixtures, and T05 completion records. Review is local
and does not delegate or implement T06 proof discharge.

## Findings resolved

1. Totality propagation was conditional on the optional control handoff. A total
   straight-line boundary root could reach a captured partial method,
   constructor or getter through data-route operations. Moved the existing
   termination derivation outside that condition. Control-only getter claims,
   exception universes and loop preparation keep their prior owner and order.
   The three actual data-route regressions import successfully and fail at
   contract attachment. Before this fix their captured partial claims were
   never propagated to the total caller, so those rejection assertions would
   fail. The all-total counterpart must still emit successfully.
2. Fixture review found that constructor/getter declaration identifiers must use
   the captured `constructor`/type name and `method`/`get_Number` identities.
   Recaptured the final data rows and required every contract's callable to
   exist before checking rejection, preventing an unrelated missing-callable
   error from satisfying the totality regression.

## Closure and import review

Both original-input attachment consumers invoke the same totality barrier.
The cumulative contract roots retain two codec origins with distinct contract
provenance. The existing T02-W02 engine alone recomputes their table and counters;
there is no new traversal for specialization or alternative limit algorithm.
Independent import checks original sidecars, bindings, obligations and complete
roots. The added cases reject removed contracts/projections/obligations, missing
codec roots and a generic-shaped function identifier at transport or import.
An optional control handoff is not made mandatory for legacy data-expressible
bodies merely to satisfy a mutation test.

The partial-loop case contains actual captured loop operations and a valid
partial loop contract, and must reach the boundary/transition rejection barrier.
The five source-firewall cases have exact owning diagnostics and zero artifacts.
The retained requests and response IDs match; accepted source facts are pinned
Linux/amd64 captures. Fuzzing targets captured protocol lineage, canonical input,
output receipt bytes and strict original sidecars with 112 bounded seeds.

Input/output artifacts preserve both contract families, source map and closed
instance linkage. The typed output fixture is reproduction evidence, not a
claim of source execution or proof. The included W04/W05 tests retain finite CLR
observations; all semantic obligations remain pending the appropriate T06 owner.

## Final review

No actionable findings remain in the task diff. `verification.json` records the
two complete composed suite runs, deterministic evidence comparison, affected
consumer tests, lint/format checks and local legacy frontend check. The source
producer and historical receipts remain unchanged.

The user explicitly waived `check-fast.sh` for this W06, citing T05-W04. No
workspace-wide substitute was run. The historical gate is not presented as
coverage of W05/W06, and the normal cadence for future tasks is unchanged.
T05 completion and T06-W01 readiness reflect this explicit task-specific waiver.
