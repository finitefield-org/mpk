# W09 unit 3 integrated-program review checkpoint

The current program extends the previous shared structural foundation with
complete source observation. The prior 42-pin review and its exact bytes are
preserved in structural-foundations/pre-observations/. Current counts are
43 actual sources, 432 unit-3 operation occurrences and 274 source-observation
carrier occurrences. This is not a unit-3 or W09 completion receipt.

The shared Builder, storage cache and concrete iteration pipeline remain within
frozen limits. Source observation has separate mode-aware type caches and
composite names, while shared scalar and decimal definitions are reused.
Semantic IEEE equality must not be replaced by bit observation. The scoped
review and 23 passing observation pairs/ten semantic contrasts are recorded in
unit-3-observation-review.md. Four standalone observation certificates passed
both unchanged checkers with zero axioms and hash-corruption rejection.

Generation, metadata/certificate mutations and exact replay passed for all
43 integrated pins. All metadata names resolve, all eligible carrier IDs have
source observations, all twelve template families are accounted for, and every
unit-3 operation is emitted, including uninvoked ones. Money/Transition and
native source-state obligations remain explicit. The nested-box case retains
the transformer-budget regression: standalone counts exceed 16,384, while the
integrated program uses 8,250 transformers.

The component comparison covers 116 original source-component pairs across
nine families, 1,160 roots and 11,207 transitive declarations. Types, bodies,
variable indices, argument order, names, levels, declaration kinds and
reducibility agree; only numerical IDs and DAG sharing are ignored. Mutating
a root or dependency body rejects. Exact standalone/integrated observation
metadata comparison additionally protects selection of the correct mode.
The 22 existing relation and two Money certificate pins remain byte-identical.

Scoped lint, format and five inventory tests pass. Both unchanged checkers
accepted all current 43 same-byte certificates with zero axioms and rejection
of all hash corruptions (1,232.277 seconds). This component acceptance does not
constitute a final zero-finding unit-3 review. Deep domain/collection semantics,
full unit-3 review and all original units 4-8 remain required. Full check-fast.sh
stays at T06-W12; no new commit or push has been made.
