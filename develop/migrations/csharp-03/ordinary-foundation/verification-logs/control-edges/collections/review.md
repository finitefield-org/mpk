# String, sequence and Option guard review

The control generator selects the original W03 definitions actually referenced
by each W04 guard. String emission reuses the extracted native-string definition
emitter. Public sequence, Option and reference adapters reuse their original
validated signatures, failure aliases and ordinary bodies. Each new metadata
table is omitted when empty, preserving the nine previous control programs
exactly. Missing constants still leave the complete guard and transport pending.

Sequence and Option foundation emission now has one owning Relations/cache when
both are selected. The previous standalone entry points delegate to the same
code with only their own family enabled. Thirty-one source contexts reproduce
their prior complete certificates and metadata: eight string, seven sequence,
twelve Option and four reference contexts. The nine old control contexts also
reproduce exactly. No prior semantic runtime or checker pass was repeated for
unchanged bytes. Visibility-only changes to sequence/reference/Option emitters
retain their bodies. Source/hash reconciliation is recorded separately.

Eight additional original loop/pattern contexts initially passed 19,681
observations. String checks include nullable absence, empty and full 16,384-unit
lengths, negative/last/first-invalid indices and ordered failure guards. Public
sequences independently check the 4,096-element capacity, including invalid large
stored lengths. Guarded transport still checks assignedness, high physical bits,
false guards and inactive payloads. Each complete program regenerates/imports,
and source/guard/state-rule/byte mutations reject. The collection partition also
compares 36 original W04 post-edge binding identities.

The type-pattern context initially retained three missing guards. Reusing the
Option value definition resolves the two invalid_operation guards, adding normal
and exceptional Some/None cases. Its final targeted test passes 1,876 observations
and leaves exactly the original modeled SwitchExpressionException edge pending.
Removing pending metadata still rejects. The initial type count of 1,806 is
superseded, not added again: the current collection partition is 17,875 unchanged
observations plus 1,876 final type observations, or 19,751. Combined with the
previous 13,910, the current total is 33,661.

All eight added certificate byte sequences pass unchanged Go/Rust checking,
zero axioms, matching reports and hash-corruption rejection. The changed type
certificate is checked separately under option/. The earlier type receipt in
cases.json is historical. Final regeneration reproduces all seventeen metadata
and certificate files exactly; final-preservation.json records each selection.
Relevant clippy and format checks pass. No Std consumer path or checker source
changed, so the inventory and whole gate were not rerun.

No remaining finding was identified in the exercised branches. The new reference
adapter selection and simultaneous option/sequence foundation path still need
dedicated original-loop coverage; standalone predecessor preservation does not
claim that coverage. The non-data type exception requires W05 execution/linkage,
not a constant-false replacement. Other unimplemented families, source execution,
node-entry merging, exceptional state composition and complete application proofs
remain open. Unit 5 and W09 remain incomplete. No component-only commit is made;
the full gate remains T06-W12.
