# Scoped pin-refresh review

Selection follows actual regenerated byte differences after the earlier domain
and aggregate corrections. It includes the integrated structural corpus and
seven immediate component families, rather than only the standalone domain
certificate. Every changed file has its original preimage and old/new hash;
the 103 certificate plan entries agree exactly with the changed-file ledger.
Original-source generation tests passed before copying candidates to the pins.
The domain/integrated pinned replay and aggregate replay then passed. Exact
transitive closure also checks every covered component against the integrated
definitions and rejects a modified root or dependency.

The dual-checker harness checks the same bytes in both implementations, requires
zero axioms and matching reports, and corrupts the certificate hash to verify
rejection. Each selected subtest must actually appear as passing in its log.
The short runner kills the whole process group at 55 seconds, so it cannot
silently leave a Rust checker running while starting another test. Timeouts are
pending checks, never semantic verdicts. Resume validates certificate and log
hashes and refuses to skip a recorded failure.

The user runner includes only those pending exact subtests. It checks the
certificate and checker-source hashes before starting, uses a fresh log
directory, records each result incrementally and stops on an error or an absent
matching subtest. Its list mode validates the handoff without executing checks.

Historical runtime and checker receipts are not rewritten as current successes.
Domain/scalar/aggregate implementation hashes match the earlier boundary-fix
record; the observer and its tests have changed since that record. Reconciliation
of that later observer scope is still required. Other downstream clause/data
pins are not covered by this refresh. No changed checker rules, new trusted
axioms, shortened physical domains or widened resource caps are introduced.

All 11 user long checks have now passed and their logs were imported without
rerunning tests. Current pin/checker-source hashes match the handoff. Together
with the 92 short passes, all 103 changed certificates are checked. The pending
list is cleared and the original manifest and timeout attempts are archived.

This review covers fixture regeneration, component closure and the bounded
handoff mechanism. The remaining downstream consumers and unit-3 scope prevent
an internal-unit completion or commit; W09 remains in progress.
