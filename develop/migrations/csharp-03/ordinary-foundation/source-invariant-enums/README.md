# Source-invariant enum comparison helpers

One ordinary definition certificate covers eight signed/unsigned underlying
types (8, 16, 32 and 64 bits), including both extrema, zero and seven. A separate
unit test executes 868 exact-arm and single-bit-flip observations against the
Rust integer oracle. It does not treat the helper inputs as imported source
facts or discharge an application proof. Real source linkage is tested by the
53-context source-invariants corpus. Current dual checker status is recorded in
../verification-logs/source-invariants/enums.json.
