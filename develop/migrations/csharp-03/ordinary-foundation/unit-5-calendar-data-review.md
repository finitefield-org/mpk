# W09 native calendar/time/Guid data relation review

No actionable finding remains in this component’s source review. Source
reconstruction and semantic checks pass. All eight same-byte dual-checker cases
subsequently passed with zero axioms and hash-corruption rejection (774.542 s).

The generator retains W03 definitions, signatures, complete typed SSA subjects,
normal/exceptional edges and formula roles. Independent regeneration compares
canonical metadata and certificate bytes on import. Signatures are matched
exactly to the existing finite Calendar/Temporal emitters; every result,
success and ordered-failure dependency closure is compared with those earlier
programs. Each admitted signature has at most one exception, so its ordered
failure predicate is also the raw condition. Money and error-outcome operations
are explicitly pending, rather than treated as a scalar successful value.

The result relation binds the computed scalar with an ordinary Let. Inside its
body computed/actual are Var(0)/Var(1), preserving lazy evaluation while sharing
work across bit comparisons. Guid needs 128-bit equality; the shared comparator
now admits this width and a new test rejects all 128 possible single-bit changes.
The 512-bit regression passes, and all nine prior integer/floating/decimal
metadata/certificate pairs are unchanged. No checker or certificate format is
modified. The Go change only registers the eight new fixtures with the existing
same-byte acceptance and hash-corruption rejection harness.

Eight original captures produce 57 definition occurrences and 67 native use
points. Every use point is checked against the original reconstructed W03
metadata. Runtime tests select the first use point for each of 52 distinct
operations and cover 124 correct/wrong-result and success/failure observations,
including invalid leap day, date overflow, time range and duration overflow.
This is not a claim of runtime enumeration of all 67 use points or all scalar
inputs. Existing frozen scalar definition closures remain identical.

The cases retain original carrier widths and source clauses. High-bit result
mutations are rejected, failed operations preserve the source guard’s vacuous
success goal, and prefix/failure guards reflect the raw exception condition.
Runtime regeneration produced exactly the same files as candidate generation.
Clippy, affected inventory closure and scoped formatting pass. Full check-fast
is deferred to T06-W12 under repository policy.

These source operation relations do not provide whole native-body, control,
ownership or application proofs. Instant normal adapters have emitter dispatch
but no fresh native case here; Instant error-outcome and Money adapters remain
separate required work. No original internal-unit completion boundary or W09
completion has been reached, and no component-only commit/push is made.
