# Closed foundation type predicates

This component links each exact W06 concrete type recipe to its fully compiled
recursive public domain. Type identities, layouts, closed parameters, dependencies,
provenance, original sequents and every proof ID are preserved. Concrete predicate
wrappers execute tag, active payload, padding, role/cumulative bounds, scalar
membership and child public clauses through ordinary definitions. Recipe hashes
only identify those definitions; they are never compared as proof goals.

Internal construction states remain explicit pending instances requiring their
separate ownership model. Partial public clauses retain the W03 definedness
requirement. Concrete operation/outcome equivalence and application proofs remain
separate pending work.

Targeted verification passes over all 45 original source contexts. Eighty-one
public type conditions are compiled and six internal-state conditions remain
pending, retaining all 87 original type obligations. The 405 semantic observations
include 325 admitted and 80 rejected values; changing only the concrete predicate
makes all 405 corresponding original conditions false. All 45 same-byte Go/Rust
cases pass with zero axioms, matching reports and hash-corruption rejection.
All 53 source-invariant metadata/certificate pins remain unchanged. Targeted
Clippy, five inventory tests and Rust/Go formatting pass.

See ../verification-logs/concrete-types/verification.json. The initial compilation
was invalidated by a reviewed test-partition correction; it supplied no verification
result. The corrected build and terminal evidence are retained separately. No
application proof is discharged. Internal unit 4 and W09 remain in progress; the
full T06 gate remains deferred to W12.
