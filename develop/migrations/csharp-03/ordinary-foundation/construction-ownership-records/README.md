# Original control-source ownership records

These three contexts extend construction ownership record checks to a loop and
normal/exceptional finally paths. `count_fill` has two records,
`array_finally_abandon` four, and `array_return_finally` two. Original captures,
function/node/action identities and exact equation lists are reconstructed in
`csharp_03_t06_w09_construction_ownership_control_records`.

All eight closed predicates execute to true and metadata with missing equations
is rejected. This is symbolic ownership evidence, not a proof of native value
execution, loop invariants, contracts or an application VC. Other data families
and application scopes remain explicitly pending in each program.

All three certificates pass identical-byte Go/Rust checking, report agreement,
zero-axiom checks and precise hash-corruption rejection.
The separate directory preserves the established six-case construction-data
corpus. The explicit-file runner calls the unchanged Go checker and Rust CLI,
compares all accepted report hashes, requires the precise hash-mismatch rejection,
and records separate deadlines and input/source/binary hashes for each stage.
See `../verification-logs/construction-data/ownership-records/verification.json`.
