# Structural clause component: direct review

Reviewed the recipe dispatch, existing semantic relation generation, cache
ownership, both consumers and exact source-capture linkage. Recipes require two
identical nominal argument IDs, empty parameters and the prescribed Bool/i32
result. The existing relation emitter determines comparison availability;
semantic equality does not become source-observation equality. Ordered checks
still reject rather than silently selecting a failure payload as a value.

The cache follows the builder and storage into public-domain and integrated
structural generation. It rejects a different VIR hash or active/nonempty target
cache. Reused scalar and structural names retain their original bodies. On recipe
failure the builder/storage are returned to their owner and the generator
propagates the error; it does not publish a partially compiled program.

The exact source request was independently captured. Signed boundary evaluation
checks both equality and comparison paths. All standalone source/public and prior
structural definition dependency closures match integrated output with a nonempty
cache; prior conditional/default/representation profiles preserve their pins.
Both new canonical candidates passed unchanged Rust/Go checking and actual
hash-corruption rejection. Current hashes and exact PASS names were reconciled.
No further component finding was identified. Remaining recipes, definedness,
native/control and application proofs remain open; this is not an internal-unit
or W09 completion review.

Final affected lint, scoped formatting, artifact-consumer closure and Git diff
whitespace checks passed. No inventory fingerprint refresh was needed.
