# Actual source for proof-level result agreement

The original C# source declares a readonly bool/float record, a constructor that
stores both operands, and a static Make method mapped to ordered_entry.make by a
content-bound sidecar. The registered offline Linux control frontend captures it
twice and requires identical bytes before writing responses.json. No MPK symbol
is present in the C# source. The capture has one accepted request.

The public full-VC generator must emit exactly one normal-commutation sequent.
Its comparison head is ObserveEqual at the concrete ordered-entry result type.
The ordinary result-agreement metadata names that exact comparison. The source
also induces Bool and float identity projections; these are tested along with
the record projection. The ordinary source-body/constructor proof remains pending;
these captures do not substitute native execution observations for a theorem.

requests.json is regenerated by csharp_03_t06_w09_binding_result_source_requests.
The captured response was produced using the existing mpk-java-t10-gate:local
image with linux/amd64, --network none, a read-only /repo mount and
scripts/build-csharp-practical-frontend.sh --test-control-emission-requests.
No toolchain or reference inventory was changed.
