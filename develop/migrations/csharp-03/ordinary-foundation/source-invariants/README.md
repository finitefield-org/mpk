# Source public-body equations

The 53 captured contexts contain 72 original W02 source-type public-body equations
and 35 original W06 source_invariant sequents. Each original formula and all
pending proof IDs are retained in certificates.json. These are definition-only
certificates; neither sample evaluations nor checker acceptance discharges a
universal application proof.

SourceShape resolves to representation-only membership. Recursive member public
domains and the exact attached public clauses remain in the original equation.
Enum arm predicates compare the complete underlying bit pattern. The ordinary
public-domain closure and representation-domain closure are preserved in full.
The strict importer regenerates canonical metadata and certificate bytes from
validated VIR, rejecting foreign identities, changed data and oversized input.

All 53 current source/import/closure cases and same-byte Go/Rust checker cases
pass, with zero axioms and hash-corruption rejection. Full original equations
pass 300 observations and 32 enum rejection cases. Nested public clauses pass
39 observations; the unchanged enum helper retains 868 verified observations.
Partial clauses still require W03 definedness and are explicitly rejected here.

Direct member views preserve shared subcubes. All 127 members, including two
with depth at least 30, match the independent storage getter after exact eta
reduction and preserve constant subcubes. The complete unlimited equation test
now passes in about ten seconds. Forty-three certificate byte sequences change;
all original source/member/type identities, public formulas and pending proof
IDs are retained. All 45 default-program metadata and certificate pins remain
unchanged. The strict importer and current targeted Clippy/format checks pass.

See ../unit-4-source-invariant-progress.json and
../verification-logs/source-invariants/direct-member-reads/verification.json.
Both test-harness failures and the interrupted obsolete equation run remain in
the evidence, separately from passing replacement checks. Partial-clause
integration, general witnesses and every application proof remain pending.
This scoped component does not complete unit 4 or W09; the full T06 gate remains
at W12.
