# Ordinary transition product definitions

These three original source contexts cover scalar and compound transition
state/events/response products. The standalone programs define make, all three
getters, semantic equality, eligible lexicographic comparison and the make
`event_bound` predicate (events argument index 1; unsigned length > 4096).
Input/public domains and source invariants remain caller obligations. Events
preserve order and duplicates. No source execution, admission, history or replay
proof is claimed.

The integrated variants reuse exactly the same metadata and transitive ordinary
definitions in the structural foundation program. All six distinct certificates
pass unchanged Go/Rust checking on identical bytes, with zero axioms and hash
corruptions rejected. See
`../verification-logs/transition-operations/verification.json` and `review.md` in
that directory for scope, commands, source hashes and preserved earlier bytes.
Unit 6 and W09 remain incomplete; the full gate stays at T06-W12.
