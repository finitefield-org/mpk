# Explicit unary reconstruction candidates

These 45 original source contexts define 37 reconstruction candidates using
explicit typed constant source completions and retain 206 original W06
conditions. The full source test passed 12,683 bit observations and four
noninjective counterexample conditions. Current generation preserves all 45
complete metadata/certificate pairs, and both unchanged checkers accept every
byte sequence with matching reports and zero axioms. Both reject changed hashes.

The remapped-boundary-sequence fixture is a negative final-proof case: changing
a stored unmapped member keeps the projection but falsifies source round-trip
and member-reconstruction conditions. A candidate definition proves no
application obligation. General semantic-dependent witnesses and universal
proof assembly remain required; this component does not complete unit 4/W09.

See `../verification-logs/binding-reconstruction/verification.json`,
`../unit-4-binding-reconstruction-progress.json` and the paired review.
