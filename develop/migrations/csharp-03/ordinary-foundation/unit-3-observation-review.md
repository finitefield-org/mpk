# Source observation component review (W09 unit 3)

The scoped review found no remaining actionable issue after the fixes below.
This is not a unit-3 or W09 completion review. Deep domain/collection semantics and original units 4-8 remain open.

The new observation path preserves the complete source stored-field product,
including fields outside an active representation arm. Semantic sums retain
active-tag/payload comparison; bounded sequences retain complete active-element
and length comparison. IEEE leaves use bit equality, so the same NaN is
reflexive and different NaN payloads/signs or floating signed zero remain
observable. Decimal cohorts and signed decimal zero use numeric equality.
Operand-domain/public-invariant and source reconstruction obligations remain
explicit caller/assembly requirements, never trusted assumptions.

Semantic equality and source observation must coexist in one ordinary program.
The type-result/active-recursion cache keys include the mode and composite
declarations use separate names. The temporary observation-mode switch restores
its prior value even when generation returns an error. Raw scalar/decimal and
iteration definitions remain shared. Existing semantic consumers explicitly
retain their previous mode and all 22 relation/two Money pins replay unchanged.

The n(id) helper's existing component users required retaining its old signature;
the observation-specific name helper is separate. The initial snapshot observer
used TaggedSum where the frozen carrier requires Option, and input validation
correctly rejected it. Fixing the test representation preserved the same values
and intended coverage; all 23 pairs and ten semantic-equality contrasts pass.
No operand-domain refusal was relabeled as an observation success.

All four observation pins pass both unchanged checkers with zero axioms and
hash-corruption rejection. Exact pin replay, source request replay, scoped lint,
format and five inventory tests pass. All 116 source-component pairs, 1,160
roots and 11,207 transitive declarations match the extended integrated program;
root/dependency mutations reject. Observation metadata is compared exactly as
well, so a warmed semantic-equality cache entry cannot silently be selected.

The previous 42-source integrated corpus, metadata and review/verification
receipt remain byte-identical in structural-foundations/pre-observations/.
Current integrated pins add the observation metadata/definitions and one new
actual source. Their 43-pin dual-checker rerun passed with zero axioms and hash-corruption rejection (1,232.277 seconds). See the
observation and integrated progress JSON files for exact versions and handles.
No new completion commit or push has been made; check-fast.sh stays at W12.
