# Original W06 representation conditions

The corpus contains 45 source-bound ordinary certificates with 120 conditions:
62 member agreements, nine exactly-one-arm conditions, 20 tag/payload agreements,
16 bounds, ten canonical-order/uniqueness conditions and three nonempty-invalid
conditions. `certificates.json` retains every original sequent, assumption/goal
function and condition function, plus the source/foundation/VC identities.

Every original binding proof obligation remains pending. Defining a Bool
implication does not prove its truth for all public values. Source invariants,
defaults, native commutation, reconstruction and complete application proofs
remain independent requirements.

The source test passed original VC equality, full guard/order/public-domain
closure preservation, unchanged guard/order bytes, canonical import, metadata
and certificate mutations and size guards across all 45 contexts. The runtime
composition test passed 385 observations from the boundary-field, bounded-
sequence, ordered-map and validation source contexts. Its oracle interprets
original Boolean connectives independently and observes preserved atomic
predicates. Twenty-seven false goals exercise the false-assumption implication
branch. Both unchanged checkers accepted all 45 complete byte sequences with matching
reports and zero axioms; both rejected every changed hash.

See `../unit-4-binding-condition-progress.json`,
`../unit-4-binding-condition-review.md` and `../verification-logs/binding-conditions/`.
This is component evidence for W09, not a completed unit-4 or W09 receipt.
