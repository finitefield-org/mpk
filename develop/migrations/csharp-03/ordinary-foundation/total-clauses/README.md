# Total source-contract observations

This component adds `sequence_length`, `tagged_is` and `parse_error_kind`.
Sequence length uses the existing complete stored-word getter through the
VIR-bound relation cache. Tagged tests use existing structural arm predicates.
ParseError kind uses its frozen u32 identity representation. Nominal signatures,
parameters and eligible templates remain checked.

The exact source/sidecar request and fresh facts are retained here. Eight source
clauses exercise both true and false predicates;four underlying functions pass
2,864 bit/tag observations. Header probes include every single-bit mask,physical
length boundaries and invalid high-bit values. Unknown tags do not alias known
arms,and changing payload bits does not change tag tests. Invalid representation
probes do not assert input public-domain membership.

All standalone clause/public-default/base structural definition closures match
integrated output. Existing structural and rich-literal candidates retain their
bytes. Both new source and integrated candidates pass unchanged Rust/Go checking
with zero axioms and hash-corruption rejection. `certificates.json` identifies
the exact checked bytes;see `../unit-4-total-clauses-progress.json` for scope,
measurements and verification selection.

The directory covers these three total observations,not the entire expression
union. Checked reads,payload extraction,constructor definedness and remaining
source/native/application proofs remain open. Passing definition certificates
does not prove the deliberately mixed true/false source invariants.
