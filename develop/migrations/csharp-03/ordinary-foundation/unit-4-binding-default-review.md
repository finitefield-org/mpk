# Binding default component review

The component regenerates the original W06 sequents from validated source VIR.
ActualDefault links only to the existing recursive structural CLR-default
candidate. Public admission flags remain separately retained and are not inputs
to any default condition. Original public-domain and semantic-arm goals are
compiled as closed Bool definitions, with no fabricated receiver, source value,
proof, or assumed use prohibition. Every original proof ID remains pending.

A default declared ineligible retains its complete original DefaultUseForbidden
sequent and an explicit source-use-proof requirement. A declared eligible value
with no representable structural default remains unresolved. All other W06
conditions are retained in the unresolved inventory for other components.

The strict importer regenerates both metadata and certificate bytes and applies
both 16 MiB guards. Targeted verification covers all 45 original source
contexts, full transitive default/projection/guard/public-domain preservation,
metadata/certificate/foreign-context rejection, actual default bit values and
negative changes that independently falsify public membership and the default
arm. Certificates must pass both local checkers with no axioms and reject hash
corruption. All 53 previous source-invariant metadata/byte pins are also replayed
under the current exports; their separate long semantic run is not restarted.

Targeted verification passed. This component does not close internal unit 4,
W09 or the full application-proof goal. The T06 gate remains deferred to W12.

The original-source generator/import/closure test now passes all 45 contexts.
Sixteen available source defaults pass 2,368 bit observations. All three
representable eligible closed conditions pass, with 157 invalid-source/matching-
arm probes and two valid-source/wrong-arm probes independently requiring both
conjuncts. All three original declaration flags remain unproven; none is a
condition dependency. The other 32 original default sequents still require
source-use proofs. Every one of the 987 original W06 proof IDs remains pending.
All 53 prior source-invariant metadata and certificate pins are unchanged under
the current build. All 45 same-byte dual-checker cases passed with zero axioms and corruption rejection. Clippy passed. The initial inventory drift is retained; adding exactly the new test preserves the prior 143-path fingerprint and passes all five inventory tests at 144 paths / 4,967 family-to-path matches. Format passed after that correction.
