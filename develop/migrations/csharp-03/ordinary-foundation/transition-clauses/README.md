# Ordinary transition contract projections (partial W09)

`transition_state`, `transition_events` and `transition_response` now select
the existing stored-product getters for a concrete semantic Transition. The
recipe checks an empty parameter object, exactly one argument, the Transition
template and arity, the selected role, and the exact nominal result type.
Event role bounds retain their complete underlying sequence representation.

Two fresh source/contract contexts cover scalar state/events with Bool response,
and a source state containing a string and integer, source-product events and
Option-of-Bool response. Six original contract expressions compare projections
with typed literal values. Each of the six distinct recipe aliases is checked
against the precise existing getter and its complete dependency closure.

The 999 core observations include full small result carriers and selected
large result carriers, plus independently addressed raw products reaching each
selector bit and the physical tail of the event carrier. Alternating patterns
distinguish neighboring fields. Raw storage probes do not claim public-domain
admission. Full collection traversal is covered by its owning implementation;
these projection tests target the changed selection and storage links.

`../unit-4-transition-clauses-progress.json` records affected consumer replay,
inventory, lint/format and the separately reported same-byte checker gate.
The source contexts were captured through the unchanged isolated data frontend.
The helper definitions do not instantiate native returned outcomes, establish
transition admission/replay conditions or discharge application VCs. Those
requirements remain in the original eight-unit W09 plan. No component-only
commit/push is made, and the whole gate remains deferred to T06-W12.

All component checks passed. Both unchanged checkers accepted the same two
candidate byte sequences with zero axioms and matching reports; actual hash
corruption rejected. Candidate hashes, sizes, metadata and tested output copies
were reconciled. The final direct review has no actionable findings.
