# Integrated structural foundations (W09 unit 3, in progress)

These 42 actual-source certificate candidates combine structural and collection
definitions in one ordinary program per validated VIR. One Builder, relation/
domain cache, concrete iteration pipeline and storage cache are shared across
generators. Existing standalone component certificates replay byte-for-byte.

Concatenating separately emitted programs would duplicate iteration pipelines
and exceed the frozen 16,384-transformer limit. The actual `nested-box` source
has 16 expanded operations; its integrated program uses 8,250 transformers,
9,637 terms and 394 declarations. Its separate domain, sequence and construction
transformer counts sum above the limit. Neither core language nor checkers change.

The corpus reaches all twelve template families. Every operation of the ten
unit-3 families is emitted, including uninvoked operations: 423 operation
occurrences across the 42 sources. Maximum costs are 47,020 terms, 666
declarations and 8,602 transformers. Complete carrier/storage, relation, domain,
default and finite unit/parse-error/exception metadata are retained. Money and
transition operations retain exact deferred instance/operation IDs with internal
owners 4 and 6. Construction ownership remains a source-state obligation.

Generation/import reconstruct exact metadata and bytes from validated VIR.
Tests check every metadata definition against its certificate, unique globals,
expanded-operation coverage, concrete limits, seventeen metadata-field mutations,
certificate corruption and pinned replay. All 42 sources passed. A composed
test executes Map Add/Lookup/nullable payload; Validation Append/Invalid/Errors/
Sequence Read; construction Allocate/Fill/Complete/Freeze/Sequence Read; complete
source-product field observation; and exact exception payload/tag observation.
All five sources and nineteen observations passed. These are observations, not
application proofs.

The checker-agreement test requires all 42 files, sends each exact byte string
to both unchanged checkers, requires zero axioms and matching reports, and
rejects a hash corruption with both checkers. All 42 passed: the original 41
in 1,685.625 seconds, then the additional nullable-string-default case in
66.921 seconds. All original 41 byte files and metadata rows remain identical.

A component-to-integrated definition comparison covers all 112 standalone
source occurrences across eight families by exact source/foundation hashes.
It compares 1,132 root definitions and 10,500 transitive declarations, ignoring
only numerical IDs and DAG sharing. Bodies, types, variable indices, argument
order, global names, universe levels, declaration kinds and reducibility agree.
Both root-body and dependency-body mutations reject. This comparison exposed the
missing nullable-string-default source, which is now included. See
`../unit-3-structural-foundation-progress.json` and the linked review for evidence.

Internal unit 3 and W09 remain incomplete. Outstanding deep-domain/collection
semantics, full unit review and original units 4-8 remain required, including
source semantics, ordinary propositions/proofs, assembly and W09 acceptance.
The full T06 gate remains deferred to T06-W12.
