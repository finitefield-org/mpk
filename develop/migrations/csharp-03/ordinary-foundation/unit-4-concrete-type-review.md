# Concrete type component review

The generator independently reconstructs the original construction/binding VCs
and closed carrier layouts from validated VIR. Each exact closed type recipe
selects its matching ordinary recursive public-domain predicate. The wrapper
executes that predicate over the actual input; it never evaluates recipe-hash
identity or an admission flag as a semantic truth. Source type, parameters,
dependencies, provenance and operation recipes remain in metadata for linkage.
The original type-equivalence sequent is lowered without rewriting its subjects,
assumptions or goals. Every application proof ID remains pending.

The shared domain/compiler implementations are unchanged. Complete transitive
public-domain comparison covers child public clauses, scalar membership,
role/cumulative limits, active payloads and padding. Strict reimport reconstructs
both canonical metadata and certificate bytes and retains both 16 MiB guards.
Foreign-context, field, certificate and input-limit mutations must reject.

Review corrected the proposed test partition: six of the original 87 concrete
type conditions belong to internal sequence-construction states. The production
code already retains these exact instances and sequents as pending ownership
work. The test now checks all original identities while distinguishing 81 public
type definitions from six pending state definitions. The earlier compilation
started before this test-only correction; its source hash guard invalidates it,
and only the corrected build may supply verification evidence.

The semantic test compares actual predicates over original source samples and
both constant physical representations. Changing only the concrete predicate in
an AST test copy must make each original equivalence condition false. These
copies are negative observations, never accepted certificates or proofs. Current
53-source invariant pins must also remain byte-identical after the exports.

Targeted verification passes. All 45 original source/import/closure cases retain
81 public conditions, six pending state conditions and all 987 proof IDs. The
semantic test passes 405 observations: 325 admitted, 80 rejected, and 405 false
conditions after changing only the concrete predicate. All 53 source-invariant
pins remain byte-identical. All 45 certificates pass both unchanged local
checkers with matching reports, zero axioms and hash-corruption rejection.
Clippy, five inventory tests and Rust/Go formatting pass. The inventory update
adds only the new test path, preserving the prior 144-path fingerprint; the new
counts are 145 namespace paths and 4,968 family-to-path matches.

The terminal audit verifies all current Rust source hashes, immutable test binary,
certificate bytes and receipt/log hashes. It retains the invalidated initial
compilation and proves its only source delta was the reviewed test correction.
See verification-logs/concrete-types/verification.json. Direct review found no
remaining issue within this component. Six construction-state type conditions,
concrete operation/outcome equivalence and all application proofs remain open.
Internal unit 4 and W09 are incomplete; the full T06 gate stays at W12.
