# Alpha Release Report

Schema: `mpk.alpha_release_report.v1`

Status: completed Go/Rust/C# successor release gate

Milestone: `CSHARP-02-T20`

## Result

The sole active release uses semantic-profile registry revision 2 and
`mpk.release.registry.v1`. Go, Rust, and C# run only through registered
descriptor-relative frontend/toolchain bundles installed beside `bin/mpk`.
Policy scan, evidence, and explanation requests use successor v2 schemas; old
public parsers, flags, staging gates, and dual-generation selectors are
unavailable.

Certificate v0 and proof acceptance are unchanged. All source, compiler,
frontend, VIR, VC, policy, AI, CI, and release-report material remains
untrusted helper evidence.

## Canonical generated report

`release-report.json` is the deterministic untrusted provenance report. It
binds both active registries, all three candidate projections, active C#
policy/AI fixtures, package certificates, both source-free checker results,
and the archived zero-finding migration records.

```sh
python3 scripts/generate-release-report.py --check
```

## Reproduction

```sh
./scripts/check-no-active-gir.sh --strict
./scripts/build-release-bundles.sh --check-build-inputs rust
./scripts/build-release-bundles.sh --check successor
./scripts/check-release-bundles.sh --fixture successor
./scripts/regenerate-go-vir-corpus.sh --check
sudo ./scripts/check-csharp-frontend.sh
./scripts/check-fast.sh
sudo ./scripts/check-all.sh
(cd go-tools/go2vir && go test -count=1 ./...)
cargo test --workspace
python3 scripts/generate-release-report.py --check
git diff --check
```

## Registered release

- registry: `mpk.release.registry.v1`;
- frontends: `frontend.go.go2vir.candidate.v1`,
  `frontend.rust.rust2vir.candidate.v2`, and
  `frontend.csharp.csharp2vir.candidate.v1`;
- toolchains: frozen Go 1.25.0, Rust nightly 2025-06-01, and
  Roslyn 5.6.0/.NET 10.0.11 candidate-v1 bundles;
- targets: Go `linux/amd64`, Rust `i686-unknown-linux-gnu` and
  `x86_64-unknown-linux-gnu`, and C# `linux-x64`;
- semantic profiles: `mpk.go.fixed.v0`, `mpk.rust.checked.v0`, and
  `mpk.csharp.scalar.v0`.

The exact registry digest and bundle inventory hashes are recorded in
`release/bundles/bundle-registry.json`; callers cannot override them.

## Review conclusions

- active Go/Rust corpora and C# policy/AI fixtures use successor identities;
- the installed release rejects predecessor, crossed, and tampered identities;
- both source-free checkers agree and the axiom inventory has no new category;
- the executable C# staging tree is absent and only reviewed migration reports
  remain under `develop/migrations/archive`;
- the final implementation review has zero findings;
- `JAVA-03` is eligible.
